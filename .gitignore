# Flutter / Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/

# Library packages should NOT commit pubspec.lock — apps that depend on
# this package will resolve their own. (Apps, on the other hand, should
# commit their lockfile.)
pubspec.lock

# IDE
.idea/
.vscode/
*.iml
*.ipr
*.iws

# macOS
.DS_Store
